name: "DiT-B"

depth: 12
hidden_size: 768 
patch_size: 1     # TODO: patch size can change based
num_heads: 12
fst_cond_type: "modulate"
snd_cond_type: "modulate"

attn_type: "jax"
grad_checkpoint: True  # grad checkpoint DiT block
# dit_block_kwargs:
#   grad_checkpoint_attn: True
#   grad_checkpoint_mlp: True
#   grad_checkpoint_adamlp: True
