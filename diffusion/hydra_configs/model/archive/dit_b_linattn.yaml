defaults:
  - dit_b

name: "DiT-B-linattn"
attn_type: "linear"
attn_kwargs:
  qk_norm: False             # default is False, but divided by self.scale
  elu: False 
  normalizer: constant
  scale_q: False