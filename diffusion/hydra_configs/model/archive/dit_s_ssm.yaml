defaults:
  - dit_s

name: "DiT-S-SSM"
attn_type: "ssm"
attn_kwargs:
  d_expansion: 1
  d_state: 128
# attn_kwargs:
#   qk_norm: False             # default is False, but divided by self.scale
#   elu: False 
#   normalizer: constant
#   scale_q: False